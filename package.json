{
  "name": "mc1-release",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "preview": "python -m http.server 4173 --directory viz",
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1"
  }
}
